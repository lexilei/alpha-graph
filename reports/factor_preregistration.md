# Factor Research Log — 10-K/10-Q text family

Two modes, by decision 2026-07-07 (supersedes the locked pre-registration in
this file's history, commit 5d4210a):

- **Explore (default, unrestricted).** Any window, any variant, any threshold,
  any diagnostic. Numbers produced here are hypotheses, not results — they may
  be quoted internally but not claimed externally.
- **Claim (only when quoting a number outside — README, interview, report).**
  At that moment, design a one-shot honest confirmation: data not used to find
  the effect, and N = the ledger count below. Until then nothing is "confirmed".

The only standing habit is the **ledger**: one line per thing tried. It
restricts nothing; it preserves the option to make a strong claim later
(an unrecorded N is unrecoverable).

Refer to factors by their permanent IDs (see `FACTORS.md`).

## Current accounting (restated 2026-07-14 after the ten-lens review; the
## authoritative count lives in the LATEST "Accounting refresh" table row)

- **Tracked count.** N = 133 (hybrid basis, see next bullet), two-sided
  ceiling `emax_null(266)` = **2.86**. Standings: C16 +3.33 (candidate,
  NOT treated as clearing — composition placebo), C17 +1.70, C15 −2.34,
  C11 −2.02 (post-price-repair quotes); C18, C20 rejected. **0 accepted.**
- **Counting-basis disclosure (review finding).** The historical count is
  a hybrid: rows counted at row level except the C16 investigation
  (counted at statistic level). Brackets: strict-row basis ≈95 → ceiling
  2.75; statistic-level basis ≈250–300 → ceiling ≈3.08, 5% family-wise
  z ≈ 3.9. No standing or verdict changes under any basis.
- **Binding rules (amended 2026-07-14):**
  1. The E[max] ceiling is a MEAN, not a significance bar (using it as a
     bar ⇒ ~43% family-wise error). External claims are graded against
     the 5% family-wise threshold at the tracked N (≈3.55 at N=133),
     and every accounting refresh must print both numbers.
  2. Every new ledger row carries `family` (XS-IC / HAC-alpha /
     event-paired / other) and `role` (sel / null). `role=null` is valid
     only when the row was pinned as a placebo/control in a
     pre-computation registration; null rows feed their family's
     empirical null pool and do NOT count toward selection-N. (This stops
     the accounting from punishing due diligence: placebo draws can never
     be promoted, so they should not raise everyone else's bar.)
  3. Pinned bars may not reference medians of fewer than 20 seeds;
     preferred form is a 99-seed empirical rank. Every registration must
     include one power/MDE sentence (forecast SE from the nearest
     realized analogue; if the MDE at 80% power exceeds the economic
     effect the bar targets, redesign before spending looks — C20's bar
     rejected its own fully-true motivating hypothesis ~90% of the time,
     and that was forecastable).
  4. Commit-then-run: registrations/pins land in their own commit before
     any result is computed; result rows quote the registration commit.
  5. Effective-N (correlation-based ~7-9) remains a redundancy
     descriptor only, never the denominator.

## Economic hypothesis (one, not many)

Lazy Prices (Cohen-Malloy-Nguyen 2020): deliberate change in a firm's periodic
filings predicts subsequent returns. All text factors are different
measurements of this single hypothesis, not independent bets.

## Context (verified on our artifacts)

- C1 standalone monthly IC, **clean corpus** (post contamination-fix
  2026-07-08, commit `0d930e0`): +0.012, t = 2.3. (Was +0.0037/t=0.68 on
  pre-fix contaminated inputs.) Incremental over the full price/volume
  baseline t=1.5 — borderline, below the promotion bar.
- ⚠ The two diagnostics below (sub-period ICs, dollar-volume-half attention
  gradient) were computed on **pre-fix** inputs and are not yet re-run:
  sub-period ICs showed no decay pattern (2012-15: t=−1.5; 2023-26: t=+2.2,
  post-hoc max over 4 buckets); no attention gradient (t=0.5 vs 0.9).
- Universe caveat: 306 of 798 tickers that passed through the index 2012–2026
  (38%) are absent (departed members) — every IC here is measured on a
  survivor-tilted cross-section, which attenuates any true signal.

## The family, by axis

| Axis | Question | Factors (variants) |
|------|----------|--------------------|
| Magnitude | how much did it change? | C1 TF-IDF, C3 finance-embed, C5 bge-embed, C7 combined-stream (rejected) |
| Direction | did tone worsen? | C4 LM tone shift |
| Added content | what's genuinely new? | C6 bge change-detect (+ BM25 lexical, if run) |
| (10-Q magnitude) | 10-Q YoY change | C2 cos_10q_yoy |

## Evaluation defaults (reference points, not gates)

- Universe: 499-ticker panel, monthly cross-sections, 2012–2026 (2011 excluded:
  no cosine pairs). Target `fwd_return_21d`; monthly cross-sectional Spearman
  IC via `factor_orthogonality.py`.
- Useful reference splits: 2012–2020 / 2021–2026; sub-period buckets as in the
  diagnostics above. None of these are binding.
- Reference bars when deciding what is *interesting*: standalone t, incremental
  t over the price/volume baseline, and DSR against the ledger N. Thresholds
  are judgment calls made per decision, not fixed in advance.
- The price/volume baseline is {B1 mom21, B2 mom5, B3 vol21, B4 volz, B5 mom12-1,
  B6 log$vol} (B5/B6 added 2026-07-07). `--sector-neutral` demeans all rank-z
  series within sector — report alongside raw as a robustness diagnostic.
  Controls are conditioning variables, not ledger trials.

## Ledger (one line per thing tried — keep appending)

> ⚠ **2026-07-08 contamination fix (commit `0d930e0`) supersedes the text-factor
> rows below dated 2026-07-08 R1 and 2026-07-07 C7 / C1 diagnostics.**
> Those were computed on inputs corrupted by a fallback-extraction switch and
> unfiltered amendments. Rows are kept (they count toward N), but their numbers
> are void. Clean re-runs: C1 standalone t=2.3 / incr t=1.5; C2 t=0.68;
> C4 t=0.72; C7 t=0.64 (all sector-neutral ≤0.55). C3/C5/C6 recomputing.

| When | What | Result |
|------|------|--------|
| 2026-07-08 | **[SUPERSEDED — pre-fix]** R1 full A/B 2012–2026, raw (standalone t / over-baseline t / over-C1 t): C1: +0.80/+0.84/— · C2: +1.46/+0.94/+1.12 · C3: +0.23/+0.70/−0.23 · C4: +0.71/+0.51/+0.98 · C5: +1.23/+1.40/+1.34 · C6: −1.02/−0.78/−1.02 | dirty inputs; void — see banner |
| 2026-07-08 | **[SUPERSEDED — pre-fix]** R1 sub-window standalone t (2012-20 / 2021-26): C1: −0.59/+1.89 · C2: +1.80/+0.18 · C3: −0.43/+0.89 · C4: +1.54/−1.14 · C5: +0.35/+1.56 · C6: +0.25/−2.09 | dirty inputs; void — see banner |
| 2026-07-08 | **[SUPERSEDED — pre-fix]** magnitude 3-way: C1 TF-IDF vs C3 finance-embed vs C5 bge-embed | dirty inputs; A/B to be re-run after C3/C5 recompute |
| 2026-07-07 | **[SUPERSEDED — pre-fix]** C7 combined freshest-filing stream | dirty inputs; clean re-run: standalone t=1.41, incr t=0.64 → still rejected |
| 2026-07-08 | **R1 re-run on clean inputs** (post `0d930e0`): incr-IC t over full baseline — C1: +1.5 · C2: +0.68 · C4: +0.72 · C7: +0.64 (sector-neutral: 1.5/0.25/0.55/0.01) | C1 now borderline (was the null); C2/C4/C7 still spanned by controls; C3/C5/C6 pending recompute |
| 2026-07-09 | **R1 COMPLETE — C3/C5/C6 recomputed clean.** incr over baseline / sector-neutral / over-C1: C3 fin +1.24/+1.96/+0.10 · C5 bge **+1.82/+2.08**/+1.39 · C6 change −1.33/−1.72/−1.23 (standalone −2.06, paper sign) | **A/B flips vs the pre-fix null**: general embedding (C5) > TF-IDF (C1) > finance-tuned (C3); C5 adds real info over C1 (t=1.39), C3 does not (0.10). C5 co-leads with C10. |
| 2026-07-09 | **Multiple-testing tally**: ~8 text/graph factors × 3 modes + A/Bs ≈ 25+ correlated looks. Max incr-t observed 1.97 (f20), 1.82 (f13); max sector-neutral 2.08 (f13) | E[max\|null] for this many correlated looks ≈ 2.0–2.3 → **no factor clears the bar**; the cluster at t≈1.5–2 is noise-compatible. Qualitative A/B structure (13>1>11) is the robust finding. |
| 2026-07-07 | diagnostic: C1 IC by sub-period (4 buckets) | no decay pattern; max bucket t=2.17 (post-hoc) |
| 2026-07-07 | diagnostic: C1 IC by dollar-volume half | no attention gradient (t=0.5 / 0.9) |
| 2026-07-07 | controls: +B5 mom_252_21, +B6 log_dollar_volume, sector-neutral mode | conditioning, not trials |
| 2026-07-08 | C8 spillover_event (full 2011-26 graph, NaN semantics) | standalone t=−0.95, vs baseline t=−0.68, sector-neutral t=+0.12 → nothing, wrong sign |
| 2026-07-08 | C9 spillover_momentum | standalone t=+0.48, incr over baseline t=+1.34 (sign matches C-F), sector-neutral halves to t=+0.64 → noise-compatible |
| 2026-07-08 | corr(18, 19) monthly rank | ~0.00 — the two propagations are independent; no combination rescue |
| 2026-07-08 | C10 spillover_cust_mom registered (customer-only, conf≥0.8, neighbors' 21d momentum) | single variant, no parameter sweep |
| 2026-07-08 | C10 full sample (165m, xs 182) | standalone t=+1.32; incr over baseline **t=+1.97** (IC +0.0142, project's largest); sector-neutral t=+1.54 — survives, unlike C9 |
| 2026-07-08 | C10 split halves (no tuning) | 2012-20: incr t=+1.61 / sn +0.89; 2021-26: +1.13 / sn +1.44 — all 4 cells positive, no post-2020 death |
| 2026-07-08 | C10 vs C9 | incr t=+1.39 — asymmetry carries info beyond the symmetric average |
| 2026-07-08 | C10 quintile L/S | +0.32%/mo, t=+1.69, 55% hit — ~1/5 of C-F's 1981-2004 magnitude, plausible for mega-caps 20y later |
| 2026-07-08 | **[SUPERSEDED — pre-fix]** R1 sector-neutral diagnostic, full panel window (vs full baseline): C2: +0.45 · C3: +1.26 · C4: +0.33 · C5: +1.54 · C6: −1.22 · C1: +0.64 | dirty inputs; void. Clean sector-neutral: C1: +1.5 · C2: +0.25 · C4: +0.55 · C7: +0.01; C3/C5/C6 pending |

| 2026-07-08 | event-window IC around filings (quarterly-clustered, EW-adjusted): 10-Q d1-5 t=−0.26 · d6-21 +0.98 · **d22-63 +2.65 (IC +0.030)** ; 10-K control: +0.06 / −1.22 / −1.05 | one cell at the ~25-look noise ceiling; 10-K does NOT replicate the pattern |
| 2026-07-08 | 10-Q d22-63 halves | 2012-19 t=+1.45, 2020-26 t=+2.29 — both positive (only text cell that doesn't sign-flip); confound: window ends at next quarter's earnings |

| 2026-07-09 | **8-K event round** (new source; 179/499 tickers with 8-K corpus). Tested ~8 constructions vs full baseline (sector-neutral t): frequency **abnormal-z −2.92 (C11)**, unscheduled-abnormal-z −3.04, raw 3mo count −1.21, raw 12mo count +1.59, hand-scored EW severity −1.49, hard-negative-item count +0.07 (C12), unscheduled density −0.99 (C13), LM sentiment +0.93 (C14) | **The signal is filing-frequency abnormality, not content.** C11 verified: split-halves −1.94/−2.89, quintile L/S +0.41%/mo t=2.55, no lookahead → candidate. Item/tone axes dead → C12–C14 rejected. Count level (vs z-score) and hand-scoring don't work. |
| 2026-07-09 | multiple-testing note for the 8-K round | ~8 correlated activity/content/tone constructions; the winner (C11 −2.92) and its unscheduled variant (−3.04) are the same idea. Treat as one axis with a winner, per protocol. Full-499 re-verification pending backfill. |

| 2026-07-10 | **Graph propagation + topology round** (7 constructions on the customer/supplier/competitor graph; vs full baseline, sector-neutral t): #1 customer-abnormal-8K-freq +0.74 · #2 customer-text(bge) +1.01 · #3 customer-volatility +0.60 · #4 supplier-momentum-upstream +1.85 · #5 competitor-momentum +0.03 · #7 customer-concentration-HHI −2.87 · #8 degree-centrality +1.62 | **Nothing survives.** #7 (HHI) was the slow-moving-factor autocorrelation trap: naive −2.87 → HAC(L=12/24) t≈−1.6, split-halves −2.39/−1.25, and 0.67-correlated with customer count (ncust itself sn +1.81) — a connectivity proxy. #3 dies under sector-neutralization (standalone 2.68 → sn 0.60 = sector effect). #4 (upstream momentum) borderline but below ceiling. **#1 pending full 8-K coverage** — it composes the two strongest ideas (C11 abnormal frequency × C10 customer channel), currently only 22% coverage. |

| 2026-07-10 | **LightGBM combiner, tuned properly** (pre-declared 9-config grid — leaves {7,15,31} × train-window {12,36,60}mo — selected on IS 2012-2020 common eval window 2017-2020, single-shot OOS 2021-2026). IS winner leaves=31/window=60m at IS Sharpe 1.87 (E[max\|null,9]=0.76) | **OOS gross 0.98 / net ~0.62 (30bp/mo), monthly t=2.24 (63mo). IS→OOS shrinkage ~50%.** Attribution (same config, OOS): baseline-only **1.02** ≥ full 0.98; novel-SEC-factors-only 0.28. The combiner is a momentum machine; the SEC factors contribute ≈ zero at portfolio level OOS. Window length (12→60mo) was the material knob, not tree complexity. |

| 2026-07-10 | **Full-corpus retest** (8-K backfill complete: 499 tickers, 100,618 filings). Sector-neutral incr t: C12 hard-items +0.08 · C13 unsched +0.24 · C14 sentiment +0.08 · unsched-abnormal-z −1.15 (was −3.04 on the 179-name subset) · composite customer-propagated-freq +0.90 (halves +0.79/+0.55, sign opposite to prediction) | C12–C14 rejections confirmed at full coverage. The unscheduled-frequency variant attenuates like C11 itself. The composite (C11 signal over C10's customer channel) does not work — the propagation round closes with nothing surviving. |

| 2026-07-10 | **Adversarial code review of the 8-K pipeline** (agent, read-only, 27 tool calls). Verdicts: C11 STANDS (no lookahead — excluding all last-calendar-day filings strengthens it to −2.94; IC broad-based, NW t=−3.22, drop-5-best −1.99), C12–C14 nulls STAND (the retest's same-month merge leak is ≤0.4% and inflationary-only), composite rejection STANDS. New characterization: C11 is a fresh-month phenomenon — fresh-attach months t=−3.68, stale months +0.35, one-month lag +1.04; decays within one month. Doc bugs fixed: split-half boundary dropped 2019 (−2.17 was 2012-18; with 2019 it is −2.07); C13's −3.04 parenthetical was subset-era (full corpus −1.15). | Review also ran one extra variant (counts toward N): hard-item abnormal-z, sn t=−0.19 standalone / +0.89 over baseline+C11 — null, reinforcing the content-axis rejection. Actionable: a daily within-month count would carry the −3.68-grade signal. |

| 2026-07-11 | **Item 1 (PIT membership) implemented** (plan v2). Curated rename map: 31 old symbols -> 18 panel tickers incl. flattened chains (SYMC/NLOK->GEN, HCP/PEAK->DOC, CBS/VIAC/PARA->PSKY, MWV/WRK->SW) and one dated reuse rule (IR<=2020-02 -> TT). Dataset facts: older renames arrive back-normalized (WLP->ANTM, AA->ARNC, GOOGL from 2010); SQ never listed (Block joined 2025-07 as XYZ, replacing HES); FISV/FI double-listed (deduped). 5 panel tickers never match (post-CSV 2026 joiners: CIEN COHR LITE SATS VRT) — excluded from PIT mode. Mask removes 331,087 rows (18.5%). Gates pass: META first PIT row 2013-12-23, TSLA 2020-12-21. | Survivor coverage by year (forward-bias fix ONLY, departed names unrecoverable): 2011 61.0% (gap 194) -> 2015 67.3% -> 2020 82.7% -> 2025 95.4% (gap 23). PIT deltas will be attributed in the 2^3 factorial with items 2+4; pit_universe defaults False until the v0 freeze. |

| 2026-07-11 | **Adversarial code review of ic_tools + pit_universe** (agent, 27 tool calls, all findings verified by running). Two HIGH: (F1) PIT mask ran BEFORE feature computation — re-entrant names' lookbacks spanned membership gaps (AMD re-entry momentum read +297% vs true +11%; 40,980 mom_252_21 rows wrong) — mask moved after features, re-entrant rows now bit-identical to non-PIT; (F2) FISV->FI map entry was INVERTED, silently dropping Fiserv for 15y (the META failure class; the gate only tested META) — fixed to FI->FISV, PIT panel 493->494 tickers. Plus: deflated_t now uses the two-sided ceiling emax_null(2N) for \|t\| selection; hac_lags override floored at the overlap minimum; DatetimeIndex split-half boundary coerced to period end; effective_n relabeled (true Li-Ji + capped variant + Nyholt over all M eigenvalues ddof=1, sparse-pair count exposed); quantile_ls tie-straddle warning; default_lags cap-truncation warning; hac_tstat degenerate-series -> NaN; ic_decay implemented. | Hard tests added: map-target-in-panel-vocabulary (would have caught FISV immediately), never-matched == known 2026 joiners, FISV 2015+2024 membership, two-sided deflation calibration (p=0.5 at ceiling), override-guardrail, hand-built exact turnover/break-even, DatetimeIndex boundary, 60/61-day carry edge. 55 tests pass. Clean-verified by the review: HAC==statsmodels to 1e-9, emax_null within 0.05 of simulation, binary search 0/5,438 property-test misses, META/TSLA gates genuinely bite. |

| 2026-07-11 | **RENAME_MAP forensic audit** (agent, 26 tool calls; identity via filing text + price-seam/level forensics). 28/31 entries CONFIRMED (incl. IR boundary exact, MMC->MRSH, PSKY chain, DISH->SATS correctly absent — mapping it would be a 6.2y false grant to old-EchoStar's line). 3 WRONG: FISV inversion (fixed by the parallel code review, verified end-to-end here); **MWV->SW and WRK->SW deleted — panel SW is the Smurfit Kappa price line (never a member; 2011 level ~9.2 vs WestRock ~26+, OTC stale fills), the mapping granted a never-member 14.1y / 3,333 rows of false membership with junk prices**. 2 MISSING added: **PX->LIN** (Praxair listing continued as Linde; 1,910 rows / 8.4y falsely excluded) and **DWDP->DD** (438-row interior hole 2017-08..2019-06). | Back-normalization boundary established: pure renames normalized upstream through ~2018-06; merger-successions NEVER normalized (produced all 4 errors) and require price-seam identity checks. Side flags for later: panel DD +17.8% on 2019-06-03 looks like a missing Corteva ex-distribution price adjustment (data-quality, task-level); filings/EXE holds pre-bankruptcy Chesapeake text under the current ticker (text-factor identity hazard); CSV double-lists KORS+CPRI 2016-2018. |

| 2026-07-13 | **build_graph dedup nondeterminism fixed.** The relationships parquet has 290 same-day duplicate groups (same source/target/filing_date, differing relation/confidence); the dedup's unstable single-key sort made the survivor depend on input row order, so the monthly and daily C10 builders got different edges — 1,217/29,048 month-end value mismatches. Fixed with a stable total order (filing_date, then confidence, then relation — latest/highest/lexicographically-last wins); regression test pins order-independence; both C10 caches rebuilt (grid invariance now 0/29,062). | Prior C10 numbers were computed on an arbitrary tie order — deltas are tie-edge-sized; full recompute lands with the factorial. |

| 2026-07-13 | factorial 1/19: C1 vs BASELINE, sector-neutral, pit=F lag=0 grid=monthly | sn incr t=+1.48 (IC +0.0066), sn raw t=+2.11, 168m, xs 451, cov 89.8% — reproduces clean-C1 ledger 1.5 |
| 2026-07-13 | factorial 2/19: C1 vs BASELINE, sn, pit=F lag=1 grid=monthly | sn incr t=+1.47 (IC +0.0065), sn raw t=+2.08, 168m, xs 451, cov 89.7% — lag ≈ inert for C1 on monthly sampling |
| 2026-07-13 | factorial 3/19: C1 vs BASELINE, sn, pit=T lag=0 grid=monthly | sn incr t=+0.29 (IC +0.0014), sn raw t=+0.74, 168m, xs 382, cov 92.5% |
| 2026-07-13 | factorial 4/19: C1 vs BASELINE, sn, pit=T lag=1 grid=monthly | sn incr t=+0.24 (IC +0.0011), sn raw t=+0.70, 168m, xs 382, cov 92.5% — **v0 cell** (C1 grid-inert: eval inputs verified bit-identical on the daily panel) |
| 2026-07-13 | factorial 5/19: C10 vs BASELINE, sn, pit=F lag=0(≡1, verified) grid=monthly | sn incr t=+1.40 (IC +0.0093), sn raw t=+1.68, 165m, xs 178, cov 34.7% — post-dedup-rebuild (was +1.54 pre-fix; tie-edge-sized delta as predicted) |
| 2026-07-13 | factorial 6/19: C10 vs BASELINE, sn, pit=T lag=0(≡1) grid=monthly | sn incr t=+1.05 (IC +0.0076), sn raw t=+1.01, 165m, xs 160, cov 38.1% |
| 2026-07-13 | factorial 7/19: C10 vs BASELINE, sn, pit=F lag=0 grid=daily | sn incr t=+1.44 (IC +0.0097), sn raw t=+1.57, 166m, xs 177, cov 34.8% |
| 2026-07-13 | factorial 8/19: C10 vs BASELINE, sn, pit=F lag=1 grid=daily | sn incr t=+1.05 (IC +0.0070), sn raw t=+1.23, 165m, xs 178, cov 34.7% |
| 2026-07-13 | factorial 9/19: C10 vs BASELINE, sn, pit=T lag=0 grid=daily | sn incr t=+0.91 (IC +0.0064), sn raw t=+1.02, 166m, xs 160, cov 38.2% |
| 2026-07-13 | factorial 10/19: C10 vs BASELINE, sn, pit=T lag=1 grid=daily | sn incr t=+0.21 (IC +0.0015), sn raw t=+0.33, 165m, xs 161, cov 38.1% — **v0 cell** |
| 2026-07-13 | factorial 11/19: C11 vs BASELINE, sn, pit=F (lag+grid verified inert for C11) | sn incr t=−2.81 (IC −0.0110), sn raw t=−3.20, 168m, xs 458, cov 96.0% — reproduces the registry headline exactly |
| 2026-07-13 | factorial 12/19: C11 vs BASELINE, sn, pit=T (lag+grid inert) | sn incr t=−2.00 (IC −0.0089), sn raw t=−2.42, 168m, xs 386, cov 97.4% — **v0-convention C11** |
| 2026-07-13 | factorial 13/19: C15 vs BASELINE, sn, pit=F lag=0 grid=daily | sn incr t=−3.29 (IC −0.0138), sn raw t=−3.47, 168m, xs 458, cov 93.1% — exceeds prior max headline \|t\| (C11 −2.81); pit=F cell, attribution only |
| 2026-07-13 | factorial 14/19: C15 vs BASELINE, sn, pit=F lag=1 grid=daily | sn incr t=−3.67 (IC −0.0156), sn raw t=−3.72, 168m, xs 458, cov 93.1% — exceeds prior max \|t\|; pit=F cell; t+1 strengthens C15 |
| 2026-07-13 | factorial 15/19: C15 vs BASELINE, sn, pit=T lag=0 grid=daily | sn incr t=−1.85 (IC −0.0090), sn raw t=−2.35, 168m, xs 386, cov 95.0% |
| 2026-07-13 | factorial 16/19: C15 vs BASELINE, sn, pit=T lag=1 grid=daily | sn incr t=−2.29 (IC −0.0111), sn raw t=−2.63, 168m, xs 386, cov 95.0% — **v0 cell**, C15's first headline |
| 2026-07-13 | factorial 17/19 (sensitivity): C1 vs BASELINE, sn, v0 panel + controls lagged 1 td (lag_controls) | sn incr t=+0.10 (IC +0.0005), sn raw t=+0.70, 168m, xs 382 — control timing moves C1 −0.14 |
| 2026-07-13 | factorial 18/19 (sensitivity): C10 vs BASELINE, sn, v0 panel + controls lagged | sn incr t=−0.05 (IC −0.0003), sn raw t=+0.34, 165m, xs 161 — moves C10 −0.26, through zero |
| 2026-07-13 | factorial 19/19 (sensitivity): C15 vs BASELINE, sn, v0 panel + controls lagged | sn incr t=−2.33 (IC −0.0112), sn raw t=−2.63, 168m, xs 386 — C15 insensitive to control timing (−0.04) |
| 2026-07-13 | v0 re-eval: C5 embed_sim_10k_bge vs BASELINE, sn, saved v0 panel (pit=T lag=1 grid=daily) | sn incr t=+1.43 (IC +0.0065, ICIR +0.11), sn raw t=+1.58 (IC +0.0074), 168m, xs 382, cov 92.5% — was sn +2.08 pre-PIT (2026-07-09); same PIT attenuation as C1; below the 1.5 borderline bar → rejected under v0 |
| 2026-07-13 | v0 re-eval: C6 new_content_frac vs BASELINE, sn, saved v0 panel | sn incr t=−0.73 (IC −0.0034, ICIR −0.06), sn raw t=−0.57 (IC −0.0029), 168m, xs 382, cov 92.5% — was sn −1.72 pre-PIT (2026-07-09) → rejected under v0 |

> **Back-fill 2026-07-13 (pre-v0 looks, dated to when they were run).** The
> 2026-07-10 README (`ac5f136`, "Holding-horizon structure") published an
> IC-decay sweep — two families across the 5–126 trading-day horizon grid
> (5/10/21/42/63/126, the grid later formalized as `ic_tools.ic_decay`) —
> but the per-cell looks never got ledger rows. They were looks under the
> counting rule; the 12 rows below repair that. Only two cells' numbers were
> preserved in the README (text @63d, C10 @10d); the rest were published as
> curve shape only (text: monotonic rise to 3–6 months; C10: peak near 10d,
> decay after). All are pre-PIT / pre-v0 attribution-era numbers.

| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 1/12: text family (C5 general-embedding quoted) @ 5d | published as curve shape only (monotonic rise); numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 2/12: text family @ 10d | published as curve shape only; numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 3/12: text family @ 21d | published as curve shape only; numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 4/12: text family @ 42d | published as curve shape only; numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 5/12: text family @ 63d | HAC incremental t ≈ +2.7 (the README's published number) |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 6/12: text family @ 126d | published as curve shape only ("rises monotonically out to a 3–6 month horizon"); numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 7/12: C10 customer momentum @ 5d | published as curve shape only (peak near 10d); numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 8/12: C10 customer momentum @ 10d | HAC incremental t ≈ +3.1 (published; also quoted in the README's C10 table) |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 9/12: C10 customer momentum @ 21d | published as curve shape only (decays after 10d); numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 10/12: C10 customer momentum @ 42d | published as curve shape only; numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 11/12: C10 customer momentum @ 63d | published as curve shape only; numeric HAC t not preserved |
| 2026-07-10 | (back-filled 2026-07-13; pre-v0) decay sweep 12/12: C10 customer momentum @ 126d | published as curve shape only; numeric HAC t not preserved |
| 2026-07-13 | **Gate-2 tradeable backtest, C15 decile L/S, registered pre-run**: long lowest-z decile, short highest-z decile (sign per C15's negative IC); monthly rebalance at each month's last trading close using that day's available signal; execution at the rebalance close (signal already availability-lagged t+1 in the v0 panel); costs base case half_spread_bps=2, commission_bps=1, borrow_bps_pa=50; three cells only: base, doubled-costs, next_open-execution. One honest pass; failure per the gate's termination rule (`reports/promotion_gate_c11.md`) is final. | registration only — no statistic; graded against the ceiling at evaluation time (N=56 → 2.57) |
| 2026-07-13 | **gate2 cell 1/3 (base)**: C15 decile L/S per the registration above — close execution, costs 2+1 bp/side + 50 bp p.a. borrow, 2011-12-30 → 2026-04-02 (172 executed rebalances, last 2026-03-31) | net Sharpe **−0.00** (gross 0.27), ann net −0.00% (gross +1.74%), maxDD −28.5%, one-way turnover 20.7x/yr, break-even 4.21 bp/side; monthly-net HAC t = −0.01 (lags 1, 173 months) vs ceiling 2.57; FF5+MOM alpha −1.55%/yr (HAC t −0.90), mkt beta −0.006, R² 0.002; legs gross: long +2.42 / short −2.17 (report: `reports/gate2_c15_tradeable.md`) |
| 2026-07-13 | **gate2 cell 2/3 (doubled costs)**: same book, all cost components x2 | net Sharpe −0.27, ann net −1.75%, maxDD −37.5% → gate criterion 4 FAIL (negative under doubling) |
| 2026-07-13 | **gate2 cell 3/3 (next_open execution)**: base costs, trade at next day's open | net Sharpe −0.00, ann net −0.01%, break-even 4.18 bp/side — execution timing immaterial; the close-execution result is not a same-close artifact |
| 2026-07-13 | **Gate-2 post-run implementation audit (not a new look)**: `run_ls_backtest` forward-filled ticker signals across PIT membership exits and target formation checked price availability but not explicit membership. The engine now accepts daily eligibility, fails closed on missing ticker-dates, and resets carry across exit/re-entry spells; regression tests cover both cases. | The three registered numerical cells remain in the ledger but are not exact PIT estimates. They already failed the frozen economics gates, so C15's live path remains closed; no bug-fix rerun may rescue it. |

Hyperparameter variants (e.g. C6 MATCH_THRESH, chunk size) get ledger
lines too when swept.

| 2026-07-13 | **C17 registration (pre-computation; not a look)**: `sue_pead` — SUE = (EPS_q − EPS_{q−4})/std(last 8 seasonal diffs, min 6); diluted as-filed first-filed EPS from `xbrl_facts.parquet`; Q4 = annual − 3 quarters where no standalone Q4; availability = earliest of Item-2.02 8-K acceptance in (period_end, filed] else statement filed date; v0 t+1 at merge. Evaluation pinned: 1 primary look (v0 judge, sn incremental over BASELINE, 21d target) + 4 decay looks (`ic_decay` @ 10/21/42/63d). Five looks total when run. | registered |
| 2026-07-13 | **C17 std-window refinement (build-time pin, pre-evaluation; not a look)**: the registration's "std(last 8 seasonal diffs)" does not state whether the current diff sits in its own null; pinned as EXCLUDING the current one — std over the trailing 8 diffs before q (min 6, ddof=1, positional over the firm's observed quarters), so the scored surprise never overlaps its own baseline (consistent with C15's non-overlapping-null design). | pinned before any evaluation |
| 2026-07-13 | **C16 registration (pre-computation; not a look)**: `insider_cluster_buy` — ≥2 distinct natural-person officers/directors, code-P open-market buys, trailing 21d; size = Σ$ / 20d ADV; entry next close after 2nd filing; hold 63td. Person-vs-entity de-clustering required (joint filings repeat rows per co-owner CIK). Event breadth ~50–120/yr → XS-IC judge cannot score it; evaluation protocol (calendar-time event portfolio vs matched baseline) to be pinned here BEFORE any computation. | registered (definition only) |
| 2026-07-13 | **C18 registration (pre-computation; not a look)**: `nt_filing_veto` — first/rare Form 12b-25 as long-book exclusion flag, not a standalone short. NT forms not yet fetched (submissions-API pass). Evaluation protocol (exclusion delta on a baseline book) to be pinned BEFORE computation. | registered (definition only) |
| 2026-07-13 | **C17 look 1/5 (primary) [pre-correction — first-2.02 availability, 132 SUE rows early-dated; corrected rerun 2026-07-14 below]**: sue_pead vs BASELINE, sn, v0 (pit=T lag=1 grid=daily, fresh panel). Build facts: 489/497 tickers with diluted EPS (25,569 first-filed quarterly obs; 28,808 rows incl. 11.2% derived Q4; 93.8% via 8-K 2.02) | sn incr t=+2.09 (IC +0.0090, ICIR +0.16), sn raw t=+1.08 (IC +0.0071), 168m, xs 370, cov 90.4%, ortho 0.923 (largest control corr mom_252_21 +0.22) — clears the pre-registered 1.5 candidate bar; below the ledger ceiling (~2.57) |
| 2026-07-13 | C17 look 2/5 (decay 10d) **[pre-correction]**: `ic_decay` on the monthly v0 panel (raw standalone IC by construction) | IC +0.0050, HAC t=+0.55 (lags 6, 176m) |
| 2026-07-13 | C17 look 3/5 (decay 21d) **[pre-correction]**: `ic_decay` on the monthly v0 panel | IC +0.0086, HAC t=+1.30 (lags 1, 176m) — the peak horizon, matching the 21d target |
| 2026-07-13 | C17 look 4/5 (decay 42d) **[pre-correction]**: `ic_decay` on the monthly v0 panel | IC +0.0042, HAC t=+0.41 (lags 4, 174m) |
| 2026-07-13 | C17 look 5/5 (decay 63d) **[pre-correction]**: `ic_decay` on the monthly v0 panel | IC +0.0002, HAC t=+0.01 (lags 4, 173m) — the drift is concentrated at ~1 month and gone by a quarter |
| 2026-07-13 | **filing_events items column (infrastructure; not a look)**: build filing_items.parquet — per-8-K item-code sets from line-anchored "Item #.##" headers, C17 ITEM_202_RE token generalized (scripts/build_filing_items.py; filing_acceptance.parquet untouched) | coverage 99.51% (100,121/100,618; zero-item files = table-mangled/glued headers and body-only mentions, not amendments); 2.02 reconciled with C17: anywhere-search 30,408 matches scan_8k_202 exactly; header-anchored 30,341 = strict subset, 67 mid-line-only mentions excluded |
| 2026-07-13 | **B7–B9 PIT fundamentals controls added (definitions in FACTORS.md; not a look)**: B7 `log_mktcap_pit` (PIT shares × close, BRK-B excluded), B8 `book_to_market_pit`, B9 `earnings_yield_pit` (as-filed first-filed XBRL; availability = the completing filing's filed date, TTM = max of the 4 constituents'; v0 t+1 at the merge; builder `signals/fundamentals_pit.py` → fundamentals_pit.parquet). No promotion testing, no evaluation looks. | BASELINE for the judge UNCHANGED (comparability with all prior looks preserved); an extended-controls set becomes available as `--accepted EXTENDED` |
| 2026-07-13 | **C19 registration (pre-computation; not a look)**: `cust_mom_pc` — the pre-registered C10-family confirmation on regulation-disclosed edges. Construction: EQUAL-weight mean of principal customers' 21d momentum; edges = `principal_customers.parquet` rows with evidence_verified AND tradeable (panel-resolvable), active in [valid_from, valid_to] (open windows stay active); daily as-of grid, computation-date stamps, v0 t+1 at merge. No confidence weighting, revenue_pct NOT used as weight (one construction, zero variants). Evaluation pinned: 3 looks — primary v0 judge (sn incremental over BASELINE) + ic_decay at {10, 21}d (10d = the original C10 claim's horizon). Success criterion pinned NOW: the family revives as candidate iff primary sn incr t ≥ +1.5 OR 10d HAC t ≥ +2.0 (original sign); anything less closes the customer-momentum family permanently. Prerequisite: backfill the 36 credit-blocked filings (WMB→YUM) so the one-shot runs on the complete corpus. Expected thin coverage (~43 panel customers / 78 suppliers): the confirmation tests sign+magnitude at pinned horizons, not ceiling clearance. | registered |
| 2026-07-13 | **C19 one-shot DEFERRED by decision (not by result)**: user parked the Together backfill + evaluation indefinitely. The registration and pinned criteria above stand unchanged; no numbers exist; the family stays in `registered` limbo until the one-shot actually runs. | deferred |
| 2026-07-13 | **C16 evaluation protocol (pre-computation pin; not a look)**: event factor, so the XS-IC judge does not apply. Calendar-time portfolio (Mitchell–Stafford): each trading day hold, equal-weight LONG-ONLY, every name whose qualifying 2nd cluster filing is within the trailing 63 trading days; entry next close after the 2nd filing (t+1). Cluster = ≥2 DISTINCT owner_ciks, each flagged officer or director, code-P open-market buys, both filings within 21 CALENDAR days; entity de-clustering pinned: exclude owner names matching corporate markers (Corp/LLC/LP/Trust/Fund/Partners/Inc/Management/Capital/Holdings/Advisors, case-insensitive). Σ$/ADV recorded as diagnostic only (no ranking — no cross-section at ~75 events/yr). Evaluation = FF5+MOM attribution of the daily portfolio return (excess=False, long-only total returns) + the same regression on each sample half: 3 looks. Success bar pinned NOW: full-sample alpha HAC t ≥ +2.0 AND positive point-estimate alpha in both halves → candidate; else rejected. | registered |
| 2026-07-13 | **C18 evaluation protocol (pre-computation pin; not a look)**: data first — fetch NT 10-K/NT 10-Q (+/A) rows via the submissions pipeline into filing_acceptance-compatible cache. Veto = 126 trading days from NT date+1, only when the firm had NO NT in the prior 3 years ("first or rare"). Evaluation = exclusion delta on the EW PIT-member monthly-rebalanced long book, WITH vs WITHOUT flagged names: 1 look. Success bar pinned NOW: exclusion improves annualized return ≥ 10bp with no Sharpe degradation → adopted as a portfolio-layer veto rule (not a factor); else rejected. Honest caveat: S&P 500 NT filings are rare; the test may be underpowered — an underpowered PASS is still not adopted (both conditions must hold on the point estimates). | registered |
| 2026-07-13 | **C18 build-time pins (pre-evaluation; not a look)**: (1) fetch — submissions API over the corpus's current-panel CIKs only (`scripts/fetch_nt_filings.py` → nt_filings.parquet; departed index members, historically the likelier late filers, are unfetchable — the veto can only ever act on surviving names, identically in both books); history from 2008-01-01 (earliest in-sample trigger 2011-04-05 minus the 3y lookback, with margin); form match = strip/upper startswith "NT 10-K"/"NT 10-Q" (covers /A and the KT/QT transition variants). (2) rule — firm key = panel ticker; same-day NT rows collapse to one trigger (earliest acceptance_ts); the 3y lookback is [t−3y, t) via DateOffset (an NT exactly 3 years earlier still blocks); blocked filings still count as history; /A amendments count as filings. (3) window — 126 trading days on the market_data calendar from the first trading day ≥ filing_date + 1 calendar day, clipped at the calendar end; triggers available before the calendar start are dropped (15, all 2008–2010 — every dropped window expires before the sample begins) and post-calendar triggers get none (1: HAS 2026-05-11). (4) book — rebalance at month-end closes with resolvable PIT membership (the 60d carry-forward bound → 179 rebalances 2011-04-29 → 2026-02-27, last book held to 2026-03-31, no stub); holdings = members with a close at the forming date; EW buy-and-hold within the month (drifting weights; halted days ffill = 0%); annualized return geometric from daily compounding; Sharpe = daily mean/std × √252, rf 0. (`signals/nt_veto.py`, `scripts/c18_exclusion_delta.py`; synthetic tests `tests/test_nt_veto.py`) | pinned before the single look |
| 2026-07-13 | **C16 de-clustering fallback (build-time pin, pre-evaluation; not a look)**: `form4_trans.parquet` carries owner_cik + role flags but NO owner name, so the registered corporate-name-marker exclusion cannot bind; nothing fetched. Pinned structural heuristic instead: qualifying owner = distinct owner_cik flagged officer/director and NOT (tenpct AND neither flag) — tenpct-only co-filers drop; inside the officer/director filter the exclusion clause is redundant, kept verbatim as the fallback rule. Known residual: joint-filing entity co-owners that carry a director flag are NOT excludable without names (80 multi-owner accessions of 9,982 qualifying repeat the same transaction lines per co-owner CIK, so their events and Σ$ stay in). | pinned before any evaluation |
| 2026-07-13 | **C16 empty-day handling (build-time pin, pre-evaluation; not a look)**: the protocol did not state what a zero-active-name day contributes; pinned as 0.0 (cash earning nothing), INCLUDED in the regression sample (a small conservative drag: excess=False still subtracts rf on those days). Series = the full market calendar 2011-04-06→2026-04-02; events outside it cannot be entered and are flagged tradeable=False (71 pre-calendar 2010-07→2011-04, 23 post-calendar). In practice 2 zero-active days of 3,770. | pinned before any evaluation |
| 2026-07-13 | **C16 look 1/3 (full sample)**: calendar-time EW long-only portfolio per the pinned protocol — 1,285 events (1,191 tradeable, 299 tickers, ~75/yr; mean 3.4 insiders/event; median Σ$/20d-ADV 0.0023, diagnostic), entry t+1 after the 2nd filing, hold 63td, series 2011-04-06→2026-04-02 (3,770d); FF5+MOM, excess=False (`signals/insider_cluster.py` → insider_cluster_c16.parquet) | alpha **+9.82%/yr (HAC t +4.20)**, mkt beta +1.004, R² 0.828; avg active names 16.2, days with ≥1 name 99.9% — clears the pinned full-sample bar (t ≥ +2.0) and sits above the ledger ceiling (~2.6) |
| 2026-07-13 | C16 look 2/3 (first half, 2011-04-06→2018-10-03): same regression on the first midpoint-date half (1,887d) | alpha +8.08%/yr (HAC t +3.08), mkt beta +1.055, R² 0.847; avg active 15.6 — positive half-sample alpha (bar condition 2 holds) |
| 2026-07-13 | C16 look 3/3 (second half, 2018-10-04→2026-04-02): same regression on the second half (1,883d) | alpha +10.99%/yr (HAC t +2.88), mkt beta +0.989, R² 0.821; avg active 16.8 — positive half-sample alpha → **C16 = candidate** per the bar pinned 2026-07-13 pre-computation |
| 2026-07-13 | **C18 look 1/1**: nt_filing_veto exclusion delta, EW PIT long book WITH vs WITHOUT vetoed names (the single pinned look; `scripts/c18_exclusion_delta.py`). Build facts: 83 NT rows 2008→2026 across 46 tickers (nt_filings.parquet), 51 first-or-rare triggers, 35 in-calendar windows (31 tickers), 23 of which veto a held member → 133 excluded name-months, 22 distinct names, avg 0.74/rebalance (179 rebalances 2011-04-29→2026-02-27, avg cross-section 388; e.g. GEN(SYMC) 2018, KHC 2019, BAX 2019-20, TFC+CCI 2020, FE 2020-21, SMCI 2024-25) | ann return WITH 13.851% vs WITHOUT 13.850% → delta **−0.14 bp/yr**; Sharpe 0.8277 vs 0.8275 → delta −0.0001; pinned bar (≥ +10 bp/yr AND no Sharpe degradation) fails on both point estimates → **rejected**. Underpowered as pre-warned — the delta rests on 23 events and is noise either way; the point estimates say the veto neither helps nor hurts this book. |
| 2026-07-13 | **C16 entry-credit disambiguation (semantics correction of the same registered hypothesis, orchestrator-authorized; not a new factor)**: the pinned "entry next close after the 2nd filing (t+1)" is ambiguous and the first implementation took the loose side — active from event_date+1 credits close(d)→close(d+1), the filing-day overnight, which a buyer AT the next close cannot capture (Form 4s are heavily after-hours; announcement reaction concentrates exactly there). Corrected reading: buy-at-next-close credits P&L from close(d+1)→close(d+2), active-return days [event_date+2, event_date+1+63], same 63-day holding length. The original-timing looks above stay ledgered (they were looks); the SAME 3 looks rerun under the corrected timing re-decide the status under the SAME pinned bar. Decomposition: the dropped first day carried **+0.51% mean / +0.38% median per event** (n 1,158) ≈ the 2.2%/yr alpha difference. Corrected series cached alongside (`insider_cluster_c16_corrected.parquet`); the original evaluated series is byte-identical on rebuild. | correction pinned before the rerun |
| 2026-07-13 | **C16 look 1/3 (corrected timing, full sample)**: same calendar-time EW long-only portfolio, first credit close(d+1)→close(d+2) (2011-04-06→2026-04-02, 3,770d), FF5+MOM excess=False | alpha **+7.67%/yr (HAC t +3.33)**, mkt beta +1.001, R² 0.829; avg active 16.2, days with ≥1 name 99.9% — still clears the pinned full-sample bar (t ≥ +2.0) |
| 2026-07-13 | C16 look 2/3 (corrected timing, first half 2011-04-06→2018-10-03, 1,887d): same regression | alpha +7.27%/yr (HAC t +2.80), mkt beta +1.057, R² 0.847; avg active 15.6 — positive half-sample alpha |
| 2026-07-13 | C16 look 3/3 (corrected timing, second half 2018-10-04→2026-04-02, 1,883d): same regression | alpha +7.48%/yr (HAC t +1.99), mkt beta +0.985, R² 0.822; avg active 16.8 — positive half-sample alpha → **C16 stays candidate under the corrected timing**; the corrected set is the headline, the original-timing set is labeled "includes the uncapturable filing-day overnight" |
| 2026-07-13 | **C16 review placebo (a look; five-lens review)**: every event date shifted +126 trading days — timing information destroyed, identical corrected-timing construction and FF5+MOM regression | α +8.18%/yr, HAC t **+3.16** (protocol-faithful full window; +6.46%/yr, t +2.89 on the placebo's populated window). Statistically indistinguishable from the headline (+7.67%, +3.33): the statistic measures the COMPOSITION of insider-bought stocks (EW small/value/anti-momentum habitat; MOM loading −0.27, t −7.9), not event timing. Its null is not zero-centered. |
| 2026-07-13 | **Accounting correction (not a new look)**: the C16 first-day-decomposition row (mean +0.51%/event, n=1,158) records computed return statistics and counts as a look under the stated rule; with it and the placebo, **N = 73**, two-sided ceiling emax_null(146) = **2.66**. Also noted by review: row-based counting understates statistic-level looks (several early rows each carry 6–18 constructions' t's; statistic-level tally ≈150–185 → ceiling ≈2.90–2.95), and the E[max] ceiling is a mean, not a significance bar — the 5% family-wise threshold at N=73 is ≈3.4. | recorded |
| 2026-07-14 | **C16 anti-evidence investigation (explore; 43 looks — every regression/paired statistic counted: D1 22 + D2 4 + D3 5 + D4 4 + D5 7 + 1 shift-construction probe; N 73 → 116, two-sided ceiling emax_null(232) = 2.81)**: how strong is the composition re-grade's against-evidence really? Corrected-timing construction throughout (`build_series` reused, FF5+MOM excess=False, full-window regression unless noted). Pre-run verification (reproductions, not looks): headline +7.67%/yr t +3.33 reproduces exactly (rebuilt series byte-identical to the cache); the ledgered +126td placebo +8.18/+3.16 reproduces exactly under its all-events searchsorted-clamped shift (71 pre-calendar events clump at pos 126); the diagnostics below use the symmetric tradeable-only shift instead (+126td → +7.88/+3.02); the one construction probe (+176-calendar-day all-events shift) gave +7.50/+2.92. | verdict **MIXED** — see the five rows; the α is composition (~+4.3%/yr) plus a slow ≥1y post-event regime (~+3.4%/yr), still nothing this calendar-time EW construction monetizes as entry timing |
| 2026-07-14 | C16 D1 — placebo DISTRIBUTION (22 looks: 6 trading-day shifts × {full, populated window} + 10 within-ticker date permutations, each ticker keeping its tradeable-event count, dates uniform over its price-coverage days, seeds 0–9). Shifts, full-window α%/yr (HAC t; n events): −252: −0.40 (−0.21; 1,073) · −126: −3.64 (−1.67; 1,120) · **−63: −23.20 (−8.76; 1,173)** · +63: +6.47 (+2.83; 1,162) · +126: +7.88 (+3.02; 1,143) · +252: +7.69 (+2.94; 1,111); populated-window: +0.14/+0.07 · −3.88/−1.78 · −23.19/−8.75 · +6.24/+2.68 · +6.07/+2.67 · +7.22/+3.07. Permutations: α +2.23…+6.62 (median +4.27), t +1.10…+3.32 (median +2.15). | real +7.67/+3.33 tops all 10 pure-composition permutations on α AND t (empirical p ≈ 1/11) and all 16 placebo t's (max +3.32, by a hair) — but on α it sits below two forward shifts (+126, +252): the null is event-relative, not flat — crash into the event, elevation ≥1y after; forward shifts are timing-contaminated nulls (they inherit the post-event regime), permutations are the honest composition null and it centers +3.4%/yr BELOW the real |
| 2026-07-14 | C16 D2 — Short-Term Reversal control (4 looks; F-F daily ST_Rev fetched with fetch_ff_factors' parsing guards, scratchpad-cached, coverage 1926→2026-05). FF5+MOM+ST_Rev α%/yr (t; st_rev β, t): real +7.31 (+3.13; +0.123, +2.32) · shift+126 +7.76 (+2.88; +0.043, +0.82) · shift−126 −3.82 (−1.74; +0.060, +3.06) · perm-seed-0 +6.54 (+2.95; +0.026, +1.01). | the real series does load on daily reversal (dip-buyer signature) but the α barely moves (−0.36%/yr) and the real-vs-placebo gaps are unchanged → NOT reversal premium in a costume; ST_Rev neither collapses the α nor widens the timing gap |
| 2026-07-14 | C16 D3 — exact composition-matched control (5 looks): per real event, same ticker at event_date − 252td; matched 663/1,191 = **55.7%** (skips: 118 pre-calendar-edge, 55 no price, 355 within ±63td of a real event). Real-matched book +7.64/+2.22 (avg 10.3 names) vs control book **−1.23/−0.55**; difference series (excess=True): full window **+8.86%/yr t +2.09**, both-populated window 2012-04→2025-06 **+8.90%/yr t +2.32**; daily corr(real, control) +0.761. | same stocks, same per-ticker frequencies, one year earlier → nothing; the paired diff is the cleanest timing estimate BUT the −252td control sits inside the pre-event decline (D1: −0.40 at −252, −23.2 at −63), so +8.9%/yr is real-minus-depressed, an UPPER bound on timing; vs the unconditional permutation null the gap is ~+3.4%/yr |
| 2026-07-14 | C16 D4 — routine vs opportunistic (4 looks; ROUTINE = ticker has cluster events in ≥6 distinct calendar months of the trailing 12 incl. current, event dates ≤ d): routine = **164 events / 3 tickers (ED 159, EQT 3, TYL 2)** — the expected plan-purchase signature; opportunistic = 1,027 events / 287 tickers. Routine book +1.51/+0.36 (≈0.9 active names, R² 0.19) vs its own +126td placebo +1.73/+0.42; opportunistic +7.63/+3.20 vs its own +126td placebo (n 979) +7.87/+2.85. | routine ≈ its placebo ≈ nothing, as expected; the headline is NOT a routine-buyer artifact (EW across ~16 names kept ED's weight small despite 88.9% day-presence) — and the opportunistic subset still equals its own forward placebo, so de-contaminating events does not rehabilitate portfolio-level timing |
| 2026-07-14 | C16 D5 — event-time CAR vs the same events shifted +126td (7 looks incl. reconciliation; paired, both windows in-calendar, n≈1,095): day **+1: +0.483% vs −0.070% (diff +0.553%, paired t +4.89)** — the known uncapturable pop; +2..+5: +0.120 vs +0.425 (diff **−0.305%**, t −1.49); **+6..+21: +1.855 vs +0.645 (diff +1.210%, t +3.04)**; +22..+63: +4.949 vs +4.111 (diff +0.838%, t +1.55). Reconciliation: credited-window (+2..+64) paired diff +1.71%/event (t +2.71) concentrates in high-concurrency events (>21 concurrent: +2.55% vs ≤21: +1.02%), yet the portfolio-level raw-mean gap is only +0.32%/yr (real +20.10 vs shifted +19.78) — calendar-time EW divides dip-clustered events by large n_active. (Paired t's are iid-across-events; window overlap inflates them.) | the capturable timing content is real but SLOW and small: nothing in +2..+5 (slightly negative), +1.2% in weeks 2–4, fading by month 3; and it lives exactly in the clustered-at-bottoms events this construction dilutes ~5× — consistent with D1's portfolio-level real ≈ forward-placebo |
| 2026-07-14 | **sue_pead availability correction (construction fix, authorized by the data review; not a look)**: last-2.02 selection replaces first-2.02 — 132 SUE rows were dated a median 23d before their EPS was computable (guidance 8-Ks under Item 2.02); C17's five pinned looks re-run below; the original rows stand, labeled. Cache rebuilt (`d1a0713`): 2,570/28,808 disclosure dates move LATER (median 39d, never earlier; 21d median among defined-SUE movers; 127 of the 132 early-dated rows move, median 23d); SUE values and via_8k02 flags unchanged; verified example AAPL 2018-12-31 now dates at the 2019-01-29 release (accepted post-16:00 → 2019-01-30), not the 2019-01-03 guidance letter | correction pinned before the rerun; pre-correction five looks reproduced exactly on the old cache first (procedure check, not looks) |
| 2026-07-14 | **C17 look 1/5 (corrected availability, primary)**: sue_pead vs BASELINE, sn, v0 (pit=T lag=1 grid=daily, fresh panel), last-2.02 cache | sn incr t=+1.69 (IC +0.0074, ICIR +0.13), sn raw t=+0.81 (IC +0.0053), 168m, xs 370, cov 90.3%, ortho 0.923 (largest control corr mom_252_21 +0.22) — was +2.09 pre-correction (part of the measured edge was the early dating); still clears the pre-registered 1.5 bar → stays candidate; below the ledger ceiling |
| 2026-07-14 | C17 look 2/5 (corrected availability, decay 10d): `ic_decay` on the monthly v0 panel (raw standalone IC by construction) | IC +0.0041, HAC t=+0.45 (lags 6, 176m) — was +0.0050/+0.55 |
| 2026-07-14 | C17 look 3/5 (corrected availability, decay 21d): `ic_decay` on the monthly v0 panel | IC +0.0074, HAC t=+1.10 (lags 1, 176m) — was +0.0086/+1.30; still the peak horizon, matching the 21d target |
| 2026-07-14 | C17 look 4/5 (corrected availability, decay 42d): `ic_decay` on the monthly v0 panel | IC +0.0024, HAC t=+0.23 (lags 4, 174m) — was +0.0042/+0.41 |
| 2026-07-14 | C17 look 5/5 (corrected availability, decay 63d): `ic_decay` on the monthly v0 panel | IC −0.0012, HAC t=−0.09 (lags 4, 173m) — was +0.0002/+0.01; the drift remains concentrated at ~1 month and is gone by a quarter |
| 2026-07-14 | **Accounting refresh**: C17's five corrected-availability looks (rerun after the last-2.02 construction fix) bring the row-based count to **N = 121, two-sided ceiling emax_null(242) = 2.83**. Post-correction standings: C17 +1.69 (candidate, barely above its 1.5 registration bar), C16 +3.33 (candidate, NOT treated as clearing per the composition re-grade), C15 −2.29, C11 −2.00. 0 accepted. | recorded |
| 2026-07-14 | **C20 registration (pre-computation; not a look)**: `insider_cluster_concurrent` — the concurrency-conditioned C16 variant the anti-evidence investigation motivates (per-event edge +1.71%/event t +2.71 concentrates in high-concurrency windows, +2.55% vs +1.02%, and EW calendar-time dilutes exactly those ~5x). Event = a C16 cluster event firing on a date whose trailing-21-calendar-day MARKET-WIDE C16 event count is ≥ the 80th percentile of its own trailing 3-year distribution (percentile computed PIT). Same corrected-timing machinery (entry close(d+1), credit from close(d+1)→close(d+2), 63td hold, EW). Expect ~20% of 1,191 events, crisis-clustered — the book is empty most of the time; this is explicitly a collective-confirmation dip strategy, not a broad factor. Evaluation pinned NOW, 6 looks: (1) FF5+MOM full sample; (2-3) halves; (4) within-ticker date-permutation placebo, median of 3 seeds; (5) same-ticker −252td matched control, paired diff; (6) THE DECISIVE ONE — same-window non-event control: random panel names (count-matched) held through the identical high-concurrency windows, answering whether insider confirmation adds anything beyond buying the dip itself. Success bar pinned: full α HAC t ≥ 2.0 AND real − permutation-median ≥ +3%/yr AND (real − same-window-control) paired t ≥ 2.0 → candidate; any miss → rejected. | registered |
| 2026-07-14 | **C20 build-time pins (pre-evaluation; not a look)**: warmup — an event with fewer than 30 prior events in its trailing-3y window (event_date ∈ (d−3y, d), DateOffset years, both ends open) does NOT qualify: the 80th percentile is not estimable early (blocks 30 of 1,285 events, 2010-07→2011-06). Micro-conventions pinned with it: the concurrency count window is (d−21 calendar days, d] — same-day events and the event itself count, an event exactly 21 days earlier does not; counts use the FULL C16 event list (non-tradeable included); the percentile is np.percentile linear interpolation over the strictly-prior events' own counts, ties qualify (≥); permutation/collision/draw conventions carried from the C16 investigation (tradeable-only event counts; collision = any of the ticker's tradeable C16 events within ±63td; paired-diff statistic = Newey-West t of the daily-diff mean, lags 32 = ff_attribution's default, full calendar; look-6 price validity over the clipped [pos0+1, pos0+64] window, one rng per look, events in (event_date, ticker) order). Builder `signals/insider_cluster_concurrent.py` (thin layer over `insider_cluster.build_series`, corrected timing); synthetic tests `tests/test_insider_cluster_concurrent.py`. | pinned before any evaluation |
| 2026-07-14 | **C20 look 1/6 (full sample)**: qualifying subset per the registered rule — 334/1,285 events qualify (**308/1,191 tradeable = 25.9%**, 178 tickers; conc_count median 11 vs full-list 6, range 6–40; ties at the discrete p80 threshold push the share above the expected ~20%); episodes (tradeable, by quarter): 2020Q1 38 · 2011Q3 37 · 2019Q3 29 · 2015Q3 21 · 2016Q1 21 · 2026Q1 20 · 2019Q2 16 · 2020Q2 12 — 2018Q4 and 2022 are present but outranked by 2019Q3/2026Q1; the book is empty half the time (days with ≥1 name 50.7%, avg 9.1 names when active, max 47; avg 4.63 over all days). Corrected-timing series 2011-04-06→2026-04-02 (3,770d), FF5+MOM excess=False (`insider_cluster_c20{,_events}.parquet`) | alpha **+3.93%/yr (HAC t +1.04)**, mkt beta +0.711, R² 0.427 — conditioning on high concurrency SHRINKS the C16 alpha (+7.67%/yr, t +3.33) instead of concentrating it; bar condition 1 (full t ≥ +2.0) **FAIL** |
| 2026-07-14 | C20 look 2/6 (first half, 2011-04-06→2018-10-03, 1,887d): same regression | alpha +1.83%/yr (HAC t +0.33), mkt beta +0.772, R² 0.356; avg active 4.14, days with ≥1 name 54.0% |
| 2026-07-14 | C20 look 3/6 (second half, 2018-10-04→2026-04-02, 1,883d): same regression | alpha +5.37%/yr (HAC t +1.09), mkt beta +0.694, R² 0.495; avg active 5.13, days with ≥1 name 47.4% |
| 2026-07-14 | C20 look 4/6 (within-ticker date-permutation placebo): tradeable qualifying events' dates permuted within ticker, uniform over each ticker's price-coverage days, rebuilt and regressed identically; seeds {0,1,2}, 308 events each | seed 0 +4.08%/yr (t +1.17) · seed 1 +1.39%/yr (t +0.44) · seed 2 +7.66%/yr (t +2.39); median +4.08%/yr (median t +1.17) → real − permutation-median = **−0.16%/yr**; bar condition 2 (≥ +3%/yr) **FAIL** — the real book does not beat its own composition null |
| 2026-07-14 | C20 look 5/6 (same-ticker −252td matched control, C16-investigation skip conventions): matched 200/308 = **64.9%** (skips: 40 calendar-edge, 14 no price, 54 within ±63td of a real C16 event); both books rebuilt on the matched subset; paired daily diff real−control, full calendar | diff mean **+4.83%/yr, HAC t +0.95** (lags 32, 3,770d) — positive point estimate, indistinguishable from zero; and the −252td control sits in the pre-event decline (C16 D3), so even this is an upper bound on timing |
| 2026-07-14 | C20 look 6/6 (**the decisive look** — same-window NON-event control, seed 0): for every tradeable qualifying event a random panel name with valid prices over the identical [d+1, d+64] window and NO C16 event within ±63td, held through the identical windows (308/308 matched, 241 distinct control names), FF5+MOM excess=False on the control book | control alpha +1.64%/yr (HAC t +0.51), mkt beta +0.655, R² 0.480; paired daily diff real−nonevent **+2.31%/yr, HAC t +0.61** (lags 32) — insider confirmation adds nothing detectable beyond buying the same dip windows; bar condition 3 (paired t ≥ +2.0) **FAIL** → all three pinned conditions miss → **C20 = rejected** |
| 2026-07-14 | **Accounting refresh**: C20's six pinned looks bring the row-based count to **N = 127, two-sided ceiling emax_null(254) = 2.84**. Standings: C16 +3.33 (candidate, not treated as clearing — composition), C17 +1.69 (candidate), C15 −2.29, C11 −2.00, C20 rejected. 0 accepted. | recorded |
| 2026-07-14 | **Price-panel repair (data fix; not a look)**: `scripts/repair_market_data.py` per the 2026-07-14 price audit — (1) wrong-identity prefixes DROPPED: SW <2024-07-08 (Smurfit Kappa OTC line, 3,333 rows), AMCR <2019-06-11 (pre-listing line, 1,778), VRT <2020-02-10 (SPAC shell, 382); (2) GDDY synthetic pre-IPO row 2015-03-31 dropped (volume 0, close = the $20 offer; 1 row); (3) INTU ZBH ZBRA ZTS re-added over the full panel window via the market.py download path (14,622 rows; ZTS from its 2013-02-01 IPO) — their absence was a yfinance batch failure the old market.py logged at DEBUG (now WARNING per ticker + summary + `attrs["failed_tickers"]`); PRICE ROWS ONLY: their filing-side corpora were never fetched (no 8-K corpus, no Form 4 rows, no XBRL/SUE except 69 INTU quarters + 3 recent INTU 10-Ks), so they enter cross-sections as target+controls rows with NaN candidates — corpus backfill is a registered follow-up; (4) six spinoff-seam name-days (DHR 2016-07-05 fake +61.2% [Fortive], EXPE 2011-12-21 −33.6% [TRIP+reverse], DD 2019-06-03 +17.8% [Corteva leg missing], LDOS 2013-09-30 +15.0% [SAIC split+reverse], HWM 2020-04-01 +7.2% vs true ≈−9..−13% [Arconic over-adjustment], HPQ 2015-11-02 +13.0%, ~8pp over [HP/HPE]) masked by NaN-ing every ret_1d/5d/21d whose window crosses the seam transition (162 cells; the consumed forward-return targets) + seam registry `market_data_seams.parquet` (ticker, seam_date, reason) for consumers to exclude. CLOSE/OHLC LEFT UNTOUCHED — price-lookback features (momentum_252_21 etc.) and close-derived books (C16/C20 calendar-time, C10 customer momentum) crossing a seam remain contaminated pending true-return splicing (registered follow-up). Panel 1,784,881 rows / 499 tickers → 1,794,009 / 503; calendar unchanged; pre-repair copy kept (`market_data_prerepair_20260714.parquet`); repair idempotent (2nd run byte-identical); synthetic + cache-invariant tests added (`tests/test_repair.py`, suite 368 → 376). Downstream rebuilds (row counts all unchanged; values refreshed): event_freq_8k_daily 1,666,181 · graph_customer_momentum_daily 1,729,500 · C16 caches (1,285 events / 1,191 tradeable / 299 tickers) · C20 caches (334 / 308 / 178; its six diagnostics reprinted — all three bar conditions still FAIL, stays rejected). The four headline quote sets re-run below as data-fix reruns per the dedup-fix/C17-correction precedent; pre-repair reproductions of all four verified exact first (procedure check, not looks). | data repair; not a look |
| 2026-07-14 | **C15 re-quote 1/6 (post-price-repair)**: the v0 judge cell (pit=T lag=1 grid=daily) on the repaired panel | sn incr t=−2.34 (IC −0.0113, ICIR −0.18), sn raw t=−2.68 (IC −0.0129), 168m, xs 386, cov 94.2% — was −2.29/−0.0111/95.0% pre-repair (Δt −0.05; the coverage dip is the re-added corpus-less names diluting the denominator) |
| 2026-07-14 | C11 re-quote 2/6 (post-price-repair): the m_p1_l0 cell (pit=T, lag=0, monthly grid) | sn incr t=−2.02 (IC −0.0090, ICIR −0.16), sn raw t=−2.44 (IC −0.0112), 168m, xs 386, cov 96.6% — was −2.00/−0.0089/97.4% pre-repair (Δt −0.02) |
| 2026-07-14 | C17 re-quote 3/6 (post-price-repair): primary, v0 judge defaults, corrected-availability (last-2.02) cache | sn incr t=+1.70 (IC +0.0074, ICIR +0.13), sn raw t=+0.81 (IC +0.0054), 168m, xs 371, cov 89.8%, ortho 0.923 — was +1.69/+0.0074/xs 370/90.3% pre-repair (Δt +0.01); stays candidate by the same 1.5 bar |
| 2026-07-14 | C16 re-quote 4/6 (post-price-repair, corrected timing, full sample 2011-04-06→2026-04-02, 3,770d): rebuilt caches, same pinned FF5+MOM regression | alpha +7.66%/yr (HAC t +3.33), mkt beta +1.001, R² 0.829, avg active 16.2 — was +7.67%/+3.33 pre-repair (Δt 0.00); the composition re-grade is untouched by the repair (the book consumes close-to-close returns and the seams stay in close) |
| 2026-07-14 | C16 re-quote 5/6 (post-price-repair, corrected timing, first half 2011-04-06→2018-10-03, 1,887d) | alpha +7.25%/yr (HAC t +2.78), mkt beta +1.057, R² 0.847 — was +7.27%/+2.80 pre-repair (Δt −0.02) |
| 2026-07-14 | C16 re-quote 6/6 (post-price-repair, corrected timing, second half 2018-10-04→2026-04-02, 1,883d) | alpha +7.48%/yr (HAC t +1.99), mkt beta +0.985, R² 0.822 — was +7.48%/+1.99 pre-repair (Δt 0.00). All six re-quotes move ≤0.05 in \|t\| (flag threshold 0.3): the audited defects were real but sat almost entirely outside the judged rank-IC windows and the insider books — the rank-z transform tames the seam outliers that did enter, and the dropped/re-added names carry few or no candidate signals |
| 2026-07-14 | **Accounting refresh**: the six post-price-repair re-quotes (data-fix reruns of the standing headline looks) bring the row-based count to **N = 133, two-sided ceiling emax_null(266) = 2.86**. Standings unchanged in substance: C16 +3.33 (candidate, not treated as clearing — composition), C17 +1.70 (candidate), C15 −2.34, C11 −2.02, C20 rejected. 0 accepted. | recorded |
| 2026-07-14 | **C21/C22 registration (pre-computation; not looks; family=XS-IC, roles=sel)**: the ONE remaining text shot, redirected per user to the PAPER'S OWN measures — `sim_minedit_10k` (C21: token-level normalized edit distance between consecutive same-type 10-Ks, Ratcliff-Obershelp/difflib ratio as the pinned implementation) and `sim_simple_10k` (C22: CMN's Sim_Simple — (adds+deletes)/(len_old+len_new) on the same token streams, sign-flipped to similarity). Fixed tokenizer (lowercase, alphanumeric word tokens), no pretrained models, same 10-K pair set as C1. ONE family A/B: any external claim is family-level; per-measure evaluation = v0 judge primary (sn incremental over BASELINE), 2 looks total. Bar pinned: family survives iff EITHER measure clears sn incr \|t\| ≥ 1.5 (else the Lazy Prices family closes under v0 for good). Power/MDE line: 168 months, IC-series vol ≈0.06 → SE(t)≈1, MDE@80% ≈ IC 0.013 (t≈2.8); the family's v0 prior is C1 +0.24 — the shot tests construction mismatch, not power. | registered |
| 2026-07-14 | **C17 window-cap guard registration (construction correction, pre-computation; the reviewer-verified F1 class)**: the 2.02-8-K search window becomes (period_end, min(stmt_filed, period_end + 95 calendar days)], fallback = stmt_filed. Rationale: LAST-in-window mis-dates ~1.6% of rows at the WRONG quarter's release when the statement is 1–4 quarters late (LDOS FY2015-Q4 dated +371d at the FY2016 release); 95d stays inside the own-quarter announcement season (next quarter's release lands ≈day 118+). The five pinned looks re-run once, tagged "(window-guard)"; review simulation forecasts primary ≈ +1.89. Family=XS-IC, roles=sel (5 looks). | registered |
| 2026-07-14 | **C23 registration (pre-computation; family=event-paired)**: `sc13d_drift` — activist-stake announcement drift on the audited sc13_filings.parquet (230 original SC 13Ds, 136 subjects, SGML-ET acceptance). Event availability: acceptance <16:00 ET → that close; ≥16:00 → next close; entry next close after availability, CAR over +1..+63td, EW across events. TWO pinned controls (roles=null): (a) same-ticker −252td matched window (skip if collides ±63td with any 13D/G event for that ticker); (b) same-window 13G-original control — for each 13D, a contemporaneous original SC 13G on a different subject (nearest acceptance date, no repeat within ±63td), isolating activist INTENT from mechanical 5%-stake demand. Looks = 3 (paired real−control t for each control + the CAR curve as one descriptive look). Bar pinned: candidate iff BOTH paired HAC t ≥ +2.0; else rejected. Power/MDE: n≈230, per-event 63td CAR SE ≈ 12%/√230 ≈ 0.8% → MDE@80% ≈ +2.2pp/event vs literature ≈ +5-7pp — adequately powered, unlike C20 (rule-3 compliance). | registered |
| 2026-07-14 | **sue_pead window-cap guard (construction fix, per the C17 window-cap guard registration commit `77ffe85`; not a look)**: the 2.02 search window upper bound becomes min(stmt_filed, period_end + 95 calendar days), keeping LAST-in-window selection and the stmt_filed fallback when no 2.02 sits in the capped window (the reviewer-verified F1 wrong-quarter class: last-2.02 uncapped dated ~1.6% of rows at the WRONG quarter's release when the statement is filed 1–4 quarters late — LDOS FY2015-Q4 at +371d; 95d stays inside the own-quarter season, next release ≈day 118+). Cache rebuilt via the module CLI: 1,593/28,808 disclosure dates move (833 EARLIER, 760 LATER, median \|shift\| 42d); among the 23,650 defined-SUE rows the panel consumes, **374 move a median 364d EARLIER** back to the own-quarter release (= 1.58% ≈ the registered ~1.6%) and 1 later; 972 rows fall from a 2.02 to the stmt_filed fallback (via_8k02 93.8%→90.4%); SUE values and derived_q4 flags byte-unchanged (verified). Guard + 3 synthetic tests added (`tests/test_sue_pead.py`: delinquent stmt_filed=+300d with 2.02s at +30d/+290d dates at +30d; normal-latency quarter inert; no-2.02-in-capped-window falls back to stmt_filed). The five pinned looks re-run below; the pre-guard five looks reproduced exactly on the old cache first (procedure check: primary +1.70, procedure bit-faithful). | construction fix pinned by the registration; cache-rebuild facts recorded (not a look) |
| 2026-07-14 | **C17 look 1/5 (window-guard, primary; family=XS-IC, role=sel)**: sue_pead vs BASELINE, sn, v0 (pit=T lag=1 grid=daily, fresh panel), last-2.02 cache capped at period_end+95d | sn incr t=**+2.01** (IC +0.0087, ICIR +0.16), sn raw t=+1.03 (IC +0.0068), 168m, xs 371, cov 89.9%, ortho 0.923 (largest control corr mom_252_21 +0.22) — was +1.70 post-price-repair (last-2.02 uncapped); the +95d cap restores ~374 delinquent quarters from the WRONG quarter's release (median 364d late) to the own-quarter release, lifting the primary (review-simulation forecast was +1.89, computed +2.01); clears the pre-registered 1.5 bar → stays candidate; below the ledger ceiling (N=138 → 2.87) |
| 2026-07-14 | C17 look 2/5 (window-guard, decay 10d; family=XS-IC, role=sel): `ic_decay` on the monthly v0 panel (raw standalone IC by construction) | IC +0.0043, HAC t=+0.47 (lags 6, 176m) — was +0.0041/+0.45 (corrected-availability) |
| 2026-07-14 | C17 look 3/5 (window-guard, decay 21d; family=XS-IC, role=sel): `ic_decay` on the monthly v0 panel | IC +0.0089, HAC t=+1.32 (lags 1, 176m) — was +0.0074/+1.10; still the peak horizon, matching the 21d target |
| 2026-07-14 | C17 look 4/5 (window-guard, decay 42d; family=XS-IC, role=sel): `ic_decay` on the monthly v0 panel | IC +0.0042, HAC t=+0.41 (lags 4, 174m) — was +0.0024/+0.23 |
| 2026-07-14 | C17 look 5/5 (window-guard, decay 63d; family=XS-IC, role=sel): `ic_decay` on the monthly v0 panel | IC +0.0006, HAC t=+0.05 (lags 4, 173m) — was −0.0012/−0.09; the drift remains concentrated at ~1 month and is gone by a quarter |
| 2026-07-14 | **Accounting refresh**: C17's five window-guard looks (rerun after the period_end+95d window-cap guard) bring the row-based count to **N = 138, two-sided ceiling emax_null(276) = 2.87** (5% family-wise threshold ≈ 3.57). Post-guard standings: C17 **+2.01** (candidate, now the headline — the last-2.02 uncapped rule's wrong-quarter staleness on ~374 delinquent quarters had suppressed real signal; above its 1.5 registration bar, below the ceiling), C16 +3.33 (candidate, NOT treated as clearing — composition), C15 −2.34, C11 −2.02, C20 rejected. 0 accepted. | recorded |
| 2026-07-14 | **C23 look 1/3 (paired real−control(a); family=event-paired, role=sel)**: `sc13d_drift` built + evaluated per the registration (`signals/sc13d_drift.py` → `sc13d_drift_events.parquet`). 230 original SC 13Ds → 204 priced (123 subjects; drops 16 pre-calendar / 4 unpriced / 3 short-window / 3 post-calendar; the 4 re-added price-only tickers INTU/ZBH/ZBRA/ZTS are not 13D subjects). Availability <16:00 ET→that close else next close; entry the next close after availability; CAR = close(entry)→close(entry+63td) holding return, EW across events. Control (a) same-ticker entry−252td; collision = any ORIGINAL 13D/13G event for the ticker within ±63td of the control anchor (originals-only reading of "13D/G event"; matched 114/204 = 55.9%, skips 58 collision / 20 edge / 12 unpriced — matches the C16 D3 55.7% precedent; an all-forms-incl-amendments collision would be 23.0%). Paired 63td CAR real−control(a), HAC t on entry-date-ordered per-event diffs, NW lag 5 (63td windows of neighbouring events overlap, mean spacing ~16td → dependence to ~4 events). role=null context: mean real +0.98% vs control(a) +3.92% | mean diff **−2.93%**, HAC t (lag 5) **−0.90** (n 114) |
| 2026-07-14 | **C23 look 2/3 (paired real−control(b); family=event-paired, role=sel)**: control (b) = greedy nearest-`acceptance_ts` ORIGINAL SC 13G on a DIFFERENT subject, no 13G filing reused and no 13G subject reused within ±63td (matched 204/204 = 100%, 148 distinct control subjects; 5,135 eligible priced 13G originals). Same availability / entry / CAR rules on the 13G's subject; same 63td paired CAR, HAC t (NW lag 5, entry-ordered). role=null context: mean real +3.72% vs control(b) +7.30% | mean diff **−3.58%**, HAC t (lag 5) **−1.67** (n 204) |
| 2026-07-14 | **C23 look 3/3 (descriptive CAR curve, excess over control(a); family=event-paired, role=sel) + BAR VERDICT**: mean cumulative real−control(a) excess at +1/+5/+21/+42/+63td = −0.19% / −0.05% / −0.16% / −1.47% / **−2.93%** (n 114; monotone-widening, all ≤0; the +63 point equals look 1's mean by construction). Pinned bar (candidate iff BOTH paired HAC t ≥ +2.0): look1 −0.90 AND look2 −1.67 → **REJECTED** (both negative, far below +2.0). Reconciliation (verification, not a look): entry = close AFTER availability excludes the announcement pop (avail-day +1.38%, median +0.59%; avail→entry overnight +0.26%); measured POP-INCLUSIVE from the pre-avail close the 13D CAR63 is +5.54% — squarely the literature's +5–7pp — so the activist premium is the announcement move, not post-entry drift (post-entry real +0.98% < +4.45% unconditional panel 63td baseline); hand-checked CARs and the NW t (== statsmodels HAC to 1e-4) reproduce exactly. Family=event-paired; these 3 role=sel looks add 3 to selection-N over the C17-refresh N=138 (C21/C22's sel looks and the final ceiling reconciled by the last committer). | rejected |
| 2026-07-14 | **C21 look 1/1 (primary; family=XS-IC, role=sel)**: `sim_minedit_10k` vs BASELINE, sn, v0 (pit=T lag=1 grid=daily, fresh panel), per the C21/C22 registration (commit `77ffe85`). Build facts: `signals/lazy_prices_edit.py` → lazy_prices_edit.parquet — C1's exact 10-K pair set (6,762 pairs, 497 tickers; `_load_filing_texts` contamination fixes + the >460d span skip), pinned tokenizer `[a-z0-9]+` on lowercased text, `difflib.SequenceMatcher(autojunk=False).ratio()`. Runtime path: full pinned `.ratio()` on 6,654 pairs (98.4%); the documented in-family fallback (10k-token chunks aligned sequentially, length-weighted) on the 108 pairs above 200k combined tokens — threshold calibrated on real pairs (~16s at 200k, ~34s at 250k vs the ~30s line; the giants are ~1.9M-token C 10-Ks) | sn incr t=**+0.06** (IC +0.0003, ICIR +0.00), sn raw t=−0.19 (IC −0.0011), 168m, xs 382, cov 91.7% (monthly-panel non-null), ortho 0.973 (largest control corr volatility_21d −0.05) — far below the 1.5 family bar |
| 2026-07-14 | **C22 look 1/1 (primary; family=XS-IC, role=sel) + FAMILY BAR VERDICT**: `sim_simple_10k` vs BASELINE, sn, v0 (same judge defaults) — CMN Sim_Simple = 1 − (adds+deletes)/(len_old+len_new) from the SAME SequenceMatcher opcodes (replace counts both sides). Identity note (mathematical, recorded at build): under the pinned implementations adds+deletes = len_old+len_new − 2·matched, so Sim_Simple ≡ `.ratio()` = 2M/T exactly — the two registered measures are the same function (verified: values identical on all 6,762 pairs, both paths); the look was still run as registered | sn incr t=**+0.06** (IC +0.0003, ICIR +0.00), sn raw t=−0.19, 168m, xs 382 — identical to C21 by the identity. Pinned family bar applied mechanically (family survives iff EITHER measure sn incr \|t\| ≥ 1.5): C21 +0.06 miss AND C22 +0.06 miss → **C21/C22 rejected; the Lazy Prices family closes under v0 for good** |
| 2026-07-14 | **Accounting refresh (the reconciliation delegated by the C23 look-3 row)**: C23's three looks + C21/C22's two primary looks bring the row-based count to **N = 143, two-sided ceiling emax_null(286) = 2.88** (5% family-wise threshold ≈ 3.58). Standings: C17 +2.01 (candidate, window-guard headline), C16 +3.33 (candidate, NOT treated as clearing — composition), C15 −2.34, C11 −2.02; C20, C21/C22, C23 rejected. 0 accepted. The Lazy Prices text family is closed under v0. | recorded |
| 2026-07-14 | **C21 digit-free tokenizer sensitivity registration (pre-computation; family=XS-IC, role=sel, ONE look)**: preprocessing-alignment audit vs CMN found the corpus already main-body-only (Items 1/1A/7; no HTML/exhibits/statement tables; full_text-fallback filings skipped) — the single deviation is digit tokens retained by `[a-z0-9]+` (MD&A digit share 6.6%, mechanical year/amount churn). Sensitivity: retokenize with `[a-z]+[a-z0-9]*` restricted to alpha-initial? NO — pinned simply as `[a-z]+` (pure-alpha tokens), rebuild sim_minedit only (C21≡C22 identity makes a second look redundant), one v0 judge look. Bar: same family rule — |t| ≥ 1.5 reopens the family; else the closure stands with preprocessing fully aligned. Power line unchanged from the C21/C22 registration (same panel, same months). | registered |
| 2026-07-14 | **C21 sensitivity look 1/1 (family=XS-IC, role=sel)**: digit-free `[a-z]+` tokenizer, sim_minedit rebuilt on all 6,762 pairs (`lazy_prices_edit_alpha.parquet`) | sn incr t = **+0.11** (IC +0.0005, raw −0.14), 168m, xs 382. Cross-sectional corr with the digit-inclusive version: **0.975** — the retained-digits deviation was immaterial by construction. Family bar (≥1.5) missed → **the Lazy Prices closure stands with preprocessing fully aligned** (main-body sections only, no HTML/exhibits/statement tables, fallback filings excluded, digits now controlled). N = 144, ceiling emax_null(288) = 2.88. |
| 2026-07-15 | **C24 registration (pre-computation; family=XS-IC; not looks)**: `sim_minedit_fresh` — the freshness-corrected Lazy Prices test (user hypothesis: as-of carry-forward dilutes a post-filing signal; 10-K-only monthly panels hold ~11/12 stale names; the paper's 10-K∪10-Q stream + monthly rebalance is effectively a rolling event window). Construction: (a) 10-Q YoY min-edit on C2's pair set (same-fiscal-quarter, ~365d, amendment/fallback filters), pinned digit-free `[a-z]+` tokenizer and C21's SequenceMatcher engine; (b) freshest-filing combined stream per C7's construction (10-K pairs ∪ 10-Q YoY pairs, as-of latest). Evaluation pinned, THREE looks: (1) PRIMARY — fresh-only judge: standard v0 monthly XS-IC with each month's cross-section RESTRICTED to names whose latest filing disclosure is ≤21 trading days old (combined stream ⇒ ~1/3 of names fresh, well above MIN_XS); (2) standard as-of judge look on the same column (dilution comparison); (3) the fresh-vs-stale contrast: judge look on the ≥63td-stale complement (role=null control — pinned now: if fresh≈stale, the dilution story is refuted). Bar pinned: family reopens iff the FRESH-ONLY look sn incr \|t\| ≥ 2.0 (stricter than 1.5: this is the third bite at the text family; a borderline fresh-only result on ~120-name cross-sections does not reopen). Power/MDE: fresh cross-sections ~120-130 names × 168m → MDE@80% ≈ IC 0.015; the hypothesis predicts concentration, not a new effect size. Universe caveat pinned: S&P 500 removes the paper's small-cap habitat; a null here refutes "dilution explains OUR zero", not the paper. | registered |
